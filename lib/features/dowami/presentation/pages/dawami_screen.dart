import 'package:flutter/material.dart';

class DawamiScreen extends StatelessWidget {
  const DawamiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('dowami'),
    );
  }
}
